a)

N = 25000;
x = -1 + 2*rand(N);
y = -1 + 2*rand(N);
plot(x,y, 'r.');

b) 

ok = (x.^2/0.81+y.^2/0.36)<=1;
xx=x(ok);
yy=y(ok);
long = length(xx);
prop = long/N;
hold on; plot(xx, yy, 'b.'); hold off

Los puntos que verifican la condicion equivale long y la proporcion de los puntos equivale a prop. 

c)

Acuadrado = 4;
Aelipse = Acuadrado * p;
Areal = pi * 0.9 * 0.6;
Erelativo = abs(Aelipse-Areal)/abs(Areal);

