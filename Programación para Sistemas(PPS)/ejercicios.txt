  GNU nano 5.4                              invertir.c
#include<stdio.h>
#include<stdlib.h>
#define M 1000
int main(){
 int arr[M];
 int elem=0;
 FILE *f;
 f=fopen("numeros.txt","r");
 if(f==NULL){
     printf("Error al leer el archivo");
     return 1;
 }else{

     while(!(feof(f))){
       int a;
       int res=fscanf(f,"%d",&a);

       fgetc(f);
       if(res!=1 || elem==M){

           while( elem>0){
             printf("%d ",arr[elem-1]);
             elem--;
             if(elem<=0){
               printf("\n");
               break;
             }
           }

       }else{
       arr[elem]=a;

       elem++;
        }
     }
 }
 printf("\n");

 return 0;
}










ecuacionde segundo grado
#include<stdio.h>
#include<math.h>
#include"ec2g.c"
int main(){

   double a, b, c,x1,x2;


   printf("\t\tIntroduzca el valor de a ");
   scanf("%lf", &a);
   printf("\t\tIntroduzcael valor de b ");
   scanf("%lf", &b);
   printf("\t\tIntroduzca el valor de c ");
   scanf("%lf", &c);

 int result=resolver(a,b,c,&x1,&x2);

 switch(result){
 case 1:
     printf("\n\t\t Caso 1: %.6f, %.6f", x1, x2);break;
 case 2:
     printf("\n\t\tCaso 2:  %.6f,  %.6f", x1, x2);break;
 case 3:
     printf("\t\tCaso 3: 0.000000, 0.000000\n");break;
 case 4:
     printf("\t\tCaso 4 :  %.6f, %.6f ",x1,x2);break;
 }
 printf("\n");
 return result;
}






matrices


#include <stdio.h>
#include <stdlib.h>
#define S 64
#define I 1
int main(){

        /*
        Para poder multiplicar matrices: (A(m,n))
                1)Sea A(m,n), B(n,p) --> n=n
                2) C=A*B; C(i,j)=sum(A(i,m)*B(n,j)

                3 -> m
                2 -> n
                3 -> p
                A(3,2)xB(2,3)

                                 1 2
                A ->    -1 0
                                -3 -1

                B ->     2 0 1
                                -5 2 3
         */
        int ma[S][S];
        int mb[S][S];
        int mc[S][S];


        int n, m, p ,i,k,j,c;
       /* int check_S=0; int check_I=0;*/
        int count=0;
        int lectura[S^3];
        FILE *f;
        f = fopen("matrices.txt", "r");

        if(f!=NULL){
        while (!(feof(f))){
                        int a;
                        int check=fscanf(f, "%i", &a);
                if(check !=1){
                        fclose(f);
                                break;
                        }
                        lectura[count]=a;
                  /*    printf("Lectura: %d\n",a);*/
                        count++;
               }

            m= lectura[0];
            n=lectura[1];
            p= lectura[2];

          /*  printf("n: %i m: %i p: %i, count: %i \n", n,m,p, count); */

     /*     int i;
            int k;*/


  /*        int ma[m][n];
            int mb[n][p];
            int mc[m][p];
  */
         /*matriz A*/
            j=1;
            c=0;
            for(i=0; i<n;i++)
                ma[0][n]=0;
                  for(i=1; i<=m;i++){
                j=1;
                          while(c<n*i){
                        ma[i][j]=lectura[c+3];
                         j++;
                         c++;
                          }
                        }


        /*      for (i=1;i<=m;i++)
                   for (j=1;j<=n;j++)
                     printf("A(%d,%d)=\t %d\n",i,j,ma[i][j]);*/

    /*matriz B*/
                for(i=0; i<p;i++)
                ma[0][p]=0;
                for(i=1; i<=n;i++){
                j=1;
                while(c-6<p*i){
                        mb[i][j]=lectura[c+3];
                         j++;
                         c++;
                          }
                      }
             /*for (i=1;i<=n;i++){
                   for (j=1;j<=p;j++){
                     printf("B(%d,%d)=\t %d\n",i,j,mb[i][j]);*/
                /*ajuste de lectura*/
                        c=0;
                        for(j=1; j<=n; j++){
                                if(ma[1][j]==0||ma[1][j]==-1){
                                        ma[1][j]=lectura[c+3];
                                        c++;
                                }
                        }

        /*producto*/
                 for (i=1;i<=m;i++) {
                        for (j=1;j<=p;j++){
                                mc[i][j]=0;
                        for (k=1;k<=n;k++){

                                        mc[i][j]=mc[i][j]+ma[i][k]*mb[k][j];
                                 }
                /*      printf("C(%d,%d): %d\n",i, j,mc[i][j]);*/
                }
        }
        for(i=1; i<=m; i++){

                for(j=1; j<=p; j++){
                        printf("%d ", mc[i][j]);
                          }
                          puts("\n");
                        }

        return 0;
         }

         else{
                puts("Error en fopen");
        return 1;
        }
}



-8 4 7
-2 0 -1
-1 -2 –6