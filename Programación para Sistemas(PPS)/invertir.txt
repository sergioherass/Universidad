#include<stdio.h>

int main(){

 int tam;
 printf("Ingresar tamaño del array :");
 scanf("%d",&tam);

 int m=1000;
 char* array[tam];
 char* array2[tam];


 for(int i=0;i<tam;i++){
     printf("Ingresar dato en array:");
     scanf("%s",&array[i]);
     // printf("%s",array[i]);
 }

 for(int i=tam-1,j=0;i>=0&&j<tam;i--,j++){

     array2[j]=array[i];
     printf("%s",array2[j]);

 }



 return 0;


}





int main(){

        int tam;
        printf("Ingresar tamaño del array :");
        scanf("%d",&tam);

        char* arr[tam];
        int n = sizeof(arr) / sizeof(arr[0]);

        for(int i=0;i<tam;i++){
             printf("Ingresar dato en array:");
             scanf("%s",&arr[i]);
     // printf("%s",array[i]);
        }

        PrintArray(arr, n);

        printf("\n");

        ReverseArray(arr, n);

        PrintArray(arr, n);

        return 0;
}





// C program to reverse an array of strings
#include <stdio.h>

int main(){
    FILE *numeros=fopen("numeros.txt","r");
    if(!numeros){
       printf("No he podido abrir el archivo");
       return 1;
    }

    char mat[14];
    int i=0;
    while(!feof(numeros)){
          fscanf(numeros,"%s",mat[i++]);
    }
     for(int i=0;i<14;i++){
         printf("%c",mat[i]);
     }

    fclose(numeros);
    return 0;




}
